#include "functions.h"
#include <stdio.h>

void printRoutes(Traveloption *trains, Traveloption *flights, int train_index1, int train_index2, int train_index3, int airplane_index1, int airplane_index2, int airplane_index3)
{
    int train_score1, train_score2, train_score3, airplane_score1, airplane_score2, airplane_score3;

    printf("train route: %s", trains[train_index1].startDest);
}
